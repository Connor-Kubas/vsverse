// window.addEventListener('DOMContentLoaded', function() {
//     const columns = document.getElementsByClassName('image-column');
  
//     for (let i = 0; i < columns.length; i++) {
//       const column = columns[i];
//       const images = column.getElementsByClassName('overlay-image');
//       let topOffset = 0;
  
//       for (let j = 0; j < images.length; j++) {
//         images[j].style.top = topOffset + '%';
//         topOffset += 20; // Adjust the value to determine the spacing between images
//       }
//     }
//   });
  